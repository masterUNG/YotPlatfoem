import 'package:flutter/material.dart';

class SellerService extends StatelessWidget {
  const SellerService({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}